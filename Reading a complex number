//Program to read complex numbers.

#include <stdio.h>

int main() 
{
    float real, imaginary;
    printf("Enter a complex number(in the format{a,b}i): ");
    scanf("{%f,%f}i", &real, &imaginary);
    printf("The complex number is: %.2f + %.2fi\n", real, imaginary);
    
    return 0;
}
